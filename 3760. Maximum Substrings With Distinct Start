class Solution{
    public int maxDistinct(String s){
        int freq[]=new int[26];
        int distinct=0;
        for(char ch:s.toCharArray()){
            if(freq[ch-'a']==0) distinct++;
            freq[ch-'a']++;
        }
        return distinct;
    }
}
