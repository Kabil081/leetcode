class Solution{
    public int minimumPrefixLength(int[] nums){
        int n=nums.length;
        int min=nums[n-1];
        for(int i=n-2;i>=0;i--){
            if(nums[i]>=min) return i+1;
            min=nums[i];
        }
        return 0;
    }
}
