class Solution {
    public List<Integer> findWordsContaining(String[] words, char x) {
        ArrayList<Integer> array=new ArrayList<>();
        for(int j=0;j<words.length;j++){
            if(words[j].indexOf(x)>=0){
                array.add(j);
            }
        }
        return array;
    }
}
